spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d6df4ti4d50c73aok0i0-a:5432/prepsphere_db}
    username: ${SPRING_DATASOURCE_USERNAME:prepsphere_db_user}
    password: ${SPRING_DATASOURCE_PASSWORD:fzw3PpUEJQtdDgJXM6wkafHXpsAGh3Rt}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

app:
  jwt:
    secret: ${APP_JWT_SECRET:change-this-to-a-long-64-byte-secret-change-this-to-a-long-64-byte-secret}
    expiration-ms: ${APP_JWT_EXPIRATION_MS:86400000}
